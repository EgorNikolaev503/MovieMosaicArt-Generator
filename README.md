# MovieMosaicArt-Generator
A Python-based tool that converts images into a mosaic using a set of movie posters. The algorithm splits the input image into 16x9 pixel blocks, determines the average color of each block, and replaces it with the most suitable color image from the provided catalog.
